[EN] Enhance Django admin panel with full model registration, inline editing, and custom filters

- Register all custom models (`User`, `Follow`, `Thread`, `Post`, `PostImage`, `Vote`, `Announcement`, `AnnouncementComment`, `AnnouncementVote`) in respective `admin.py` files.
- Customize `UserAdmin` with custom roles/avatars fieldsets, search fields, filters, and admin actions (make_moderator, make_admin).
- Add `PostInline` and custom annotations (`posts_count`) in `ThreadAdmin`.
- Implement `list_editable` for announcements and optimized search/filters across all admin modules.

[UA] Покращення адмін-панелі Django: реєстрація всіх моделей, inline-редагування та кастомні фільтри

- Зареєстровано всі кастомні моделі (`User`, `Follow`, `Thread`, `Post`, `PostImage`, `Vote`, `Announcement`, `AnnouncementComment`, `AnnouncementVote`) у відповідних файлах `admin.py`.
- Налаштовано `UserAdmin` із додаванням полів ролей та аватарок, розширеним пошуком, фільтрами та кастомними діями (make_moderator, make_admin).
- Додано `PostInline` та анотації кількості постів (`posts_count`) у `ThreadAdmin`.
- Реалізовано `list_editable` для оголошень та оптимізовано пошук і фільтри у всіх модулях адмінки.
